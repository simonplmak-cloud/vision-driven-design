{
  "name": "vision-driven-design",
  "private": true,
  "scripts": {
    "build": "mkdir -p public && cp index.html public/",
    "build:packages": "pnpm -r build",
    "check": "pnpm -r check",
    "test": "pnpm -r test"
  }
}
